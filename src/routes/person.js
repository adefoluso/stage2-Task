const express = require("express");

const PersonController = require("../controllers");

const router = express.Router();

// router.post("/users", PersonController.createUser);

module.exports = router;
